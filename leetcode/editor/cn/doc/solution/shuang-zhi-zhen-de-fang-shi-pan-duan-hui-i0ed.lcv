### 解题思路

此处撰写解题思路

### 代码

* java

```java
class Solution {
    public boolean isPalindrome(int x) {
		String hw = String.valueOf(x);
		char[] char1 = hw.toCharArray();
		
		int s = 0;
		int e = char1.length - 1;
		while(s < e) {
			if(char1[s] != char1[e]) {
				return false;
			}
			s++;
			e--;
		}
		return true;
    }
}
```

